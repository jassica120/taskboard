'use client';

import {useRouter} from 'next/navigation';
import { useEffect, useState } from 'react'; //一定會用到
import axios from 'axios'; // 這行是用來引入 axios 函式庫的，


export default function Airline() {
    const getAcessToken = async () => {
        
        console.log(ponse);

        
    }

    const respone = await ()-> {

    };

    return(

    )
}